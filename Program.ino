
// Define stuff here
int i = 0

// This is called once, on startup
void setup() {
  // Do pin assignment or other stuff here
}

// This loop is called continuously
void loop(void) {
  // So, a busy wait - is that all you can think of ?
  delay(1000);
}
